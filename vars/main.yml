# vars/main.yml

# The username on the target z/OS system (e.g. omvsadm)
TARGET_USERNAME: <remote z/OS username>

# Name of the SSH key (e.g. git_ssh_key)
KEY_TITLE: <name of the SSH key>

# Location of the SSH key to be (e.g. /u/{{ TARGET_USERNAME }}/.ssh/id_rsa_git)
KEY_PATH: /path/to/ssh/key

# # GitHub personal access token
GITHUB_ACCESS_TOKEN: <Your Encrypted GitHub Token>

# Git repository to be cloned. Must be SSH (not https)
GIT_REPO: ssh://git@github.com/path/to/repository.git

# Branch of the git repository (e.g. master)
GIT_BRANCH: <repository branch>

# Directory where the repository will be cloned (e.g. /u/{{ TARGET_USERNAME }}/repo_name)
CLONE_DEST: /path/to/clone/directory

# Location of the SSH known hosts file (e.g. /u/{{ TARGET_USERNAME }}/.ssh/known_hosts)
KNOWN_HOSTS_PATH: /path/to/ssh/known_hosts/file

# Location of the git binary (e.g. /hsstools/git-2.14.4/bin/git)
GIT_EXECUTABLE: /path/to/git/executable